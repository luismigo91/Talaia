# Tareas: retención de series y CI completo

- [ ] `db/migrations/0009_retencion.sql`: columnstore y políticas en `observations` y `forecasts`.
- [ ] Test de integración que comprueba las políticas activas tras migrar.
- [ ] CI: `next build` y construcción de las imágenes `api`, `collectors` y `web`.
- [ ] Suite completa en verde.
- [ ] Archivar y fusionar en `openspec/specs/` (tras validación).
